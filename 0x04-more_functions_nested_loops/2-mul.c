#include "main.h"

/**
  * mul - function  performs multiplication on two numbers
  * @a: first number to taken as argument
  * @b: second number to be taken as argument
  * Return: returns  the product results
  */

int mul(int a, int b)
{
	return (a * b);
}

